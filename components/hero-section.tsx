'use client';

import { HeroFuturistic } from '@/components/ui/hero-futuristic';

export default function HeroSection() {
  return (
    <section id="hero" className="relative w-full overflow-hidden bg-background min-h-svh">
      <HeroFuturistic />
    </section>
  );
}
